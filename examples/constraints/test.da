from pprint import pprint

def constraint(name='test', variable={a,b}):
	a: int
	b: int
	def c1():
		a > b
		a + b == 10
		a >= 0
		b >= 0

	return setof(a+b, c1)

result = query(constraint='test')
pprint(result)