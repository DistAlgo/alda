import time
class P (process):
   def setup(): pass
   def run():
       output('====')
       time.sleep(1)
       output(time.time())
       if await False: pass
       elif timeout(2): pass
       output(time.time())

       await(timeout(2))
       output(time.time())
#        await timeout(2)
       output(time.time())
#        await timeout(time=2)
       output(time.time())

def main():
   p = new(P,[])
   start(p)